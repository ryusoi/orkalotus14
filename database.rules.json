{
  "rules": {
    ".read": true,
    ".write": true,
    "ratings": {
      "$ratingId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['id', 'targetId', 'overallRating'])"
      }
    }
  }
}
